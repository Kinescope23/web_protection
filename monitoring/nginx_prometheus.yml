# Метрики для Prometheus
server {
    listen 9113;
    location /stub_status {
        stub_status on;
        access_log off;
        allow 172.16.0.0/12;  # Docker networks
        allow 127.0.0.1;
        deny all;
    }
}